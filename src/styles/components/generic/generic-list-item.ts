import { createStyles } from "@material-ui/core";

export default createStyles({

  card: {
    display: "flex",
    flexDirection: "column",
    borderRadius: 0
  },

  list: {
    display: "flex",
    borderRadius: 0
  },

  cardContent: {

  },

  imageWrapper: {
    width: "100%",
    height: 200,
    overflow: "hidden"
  },

  cardImage: {
    maxWidth: "100%",
    maxHeigh: "100%"
  }

});