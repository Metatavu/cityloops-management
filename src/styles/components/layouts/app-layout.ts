import { createStyles } from "@material-ui/core";

export default createStyles({

  root: {
    width: "100vw",
    height: "100vh",
    overflowX: "hidden"
  },
  scrollWrapper: {
    overflowY: "auto"
  },
  contentWrapper: {
    height: "100%"
  }

});